generator client {
  provider = "prisma-client"
}

datasource db {
  provider = "mysql"
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model attendance_logs {
  log_id       Int                     @id @default(autoincrement()) @db.UnsignedInt
  uid          Int                     @db.UnsignedInt
  course_code  String                  @db.VarChar(6)
  semester     Int                     @db.UnsignedTinyInt
  slotID       Int                     @db.UnsignedInt
  lecture_date DateTime                @db.Date
  status       attendance_logs_status?
  courses      courses                 @relation(fields: [course_code], references: [course_code], onDelete: Cascade, onUpdate: Restrict, map: "fk_course")
  time_slots   time_slots              @relation(fields: [slotID], references: [slotID], onDelete: Cascade, onUpdate: Restrict, map: "fk_slot")
  users        users                   @relation(fields: [uid], references: [uid], onDelete: Cascade, onUpdate: Restrict, map: "fk_uid")

  @@index([course_code], map: "fk_course")
  @@index([slotID], map: "fk_slot")
  @@index([uid], map: "fk_uid")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model course_attendance {
  uid           Int     @db.UnsignedInt
  course_code   String  @db.VarChar(6)
  semester      Int     @db.UnsignedTinyInt
  present_total Int     @default(0) @db.UnsignedInt
  absent_total  Int     @default(0) @db.UnsignedInt
  medical_total Int     @default(0) @db.UnsignedInt
  total_classes Int     @default(0) @db.UnsignedInt
  courses       courses @relation(fields: [course_code], references: [course_code], onDelete: Cascade, onUpdate: Restrict, map: "fk_attendance_course")
  users         users   @relation(fields: [uid], references: [uid], onDelete: Cascade, onUpdate: Restrict, map: "fk_attendance_user")

  @@id([uid, course_code])
  @@index([course_code], map: "fk_attendance_course")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model courses {
  course_code       String              @id @db.VarChar(6)
  course_name       String              @db.VarChar(150)
  semester          Int                 @db.UnsignedTinyInt
  branch            String              @db.VarChar(100)
  attendance_logs   attendance_logs[]
  course_attendance course_attendance[]
}

model time_slots {
  slotID          Int                    @id @default(autoincrement()) @db.UnsignedInt
  day_of_week     time_slots_day_of_week
  start_time      DateTime               @db.Time(0)
  end_time        DateTime               @db.Time(0)
  attendance_logs attendance_logs[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model users {
  uid               Int                 @id @default(autoincrement()) @db.UnsignedInt
  email             String              @unique(map: "email") @db.VarChar(50)
  first_name        String              @db.VarChar(50)
  last_name         String?             @db.VarChar(50)
  roll_number       String              @unique(map: "roll_number") @db.VarChar(10)
  branch            String              @db.VarChar(100)
  semester          Int                 @db.UnsignedTinyInt
  image_url         String?             @db.VarChar(200)
  batch             DateTime            @db.Date
  attendance_logs   attendance_logs[]
  course_attendance course_attendance[]
}

enum time_slots_day_of_week {
  MON
  TUE
  WED
  THU
  FRI
  SAT
  SUN
}

enum attendance_logs_status {
  present
  absent
  medical
}
